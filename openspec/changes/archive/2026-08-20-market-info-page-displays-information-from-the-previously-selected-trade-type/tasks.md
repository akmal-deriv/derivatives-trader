## Open Questions

None.

## 1. Fix — resync browse trade type + clear stale info state

- [x] 1.1 In `packages/trader/src/AppV2/Hooks/useMarketSelection.ts`, add `useEffect` to the React import (already imports `useCallback, useMemo, useState` on line 1) and add an effect inside `useMarketSelection` keyed on the store's `contract_type` (destructured at line 35) that: (a) calls `setSelectedTradeType(getTradeTypeForContractType(contract_type) ?? AVAILABLE_CONTRACTS[0])`, and (b) clears stale info state via `setInfoSymbol(null)` and `setInfoTradeType('')`. `getTradeTypeForContractType` and `AVAILABLE_CONTRACTS` are already imported (lines 12, 18). Keep the existing lazy `useState` initialiser at lines 37-39 as the mount seed.
- [x] 1.2 Verify `handleSelectTradeType` (lines 147-150) is unchanged — it must still only call `setSelectedTradeType(contract)` (local state), so a voluntary browse-tab pick survives until the store's `contract_type` changes (the resync effect's trigger).

## 2. Tests — regression for the resync

- [x] 2.1 In `packages/trader/src/AppV2/Hooks/__tests__/useMarketSelection.spec.tsx`, extend the `useTraderStore` mock (lines 40-43) so `contract_type` is mutable across renders (e.g. a module-level `let mock_contract_type = 'rise_fall'` and `useTraderStore: jest.fn(() => ({ contract_type: mock_contract_type, selectMarketAndTradeType: jest.fn() }))`), mirroring the existing mutable-mock pattern used for `useActiveSymbols`/`useFavouriteMarkets`. Reset `mock_contract_type = 'rise_fall'` in `beforeEach`.
- [x] 2.2 Add a test `describe('browse trade type resyncs to the live store contract type')` that: renders the hook with `mock_contract_type = 'rise_fall'`, asserts `result.current.current_trade_type` is the Rise/Fall tab; then sets `mock_contract_type = 'accumulator'` and `rerender`s, and asserts `result.current.current_trade_type` is the Accumulators tab (not Rise/Fall).
- [x] 2.3 Add a test that after `result.current.showInfo('R_100', 'Rise/Fall')` (so `info_symbol` is set), setting `mock_contract_type = 'accumulator'` and `rerender`ing clears `result.current.info_symbol` to `null` and `result.current.info_trade_type` to `''`.
- [x] 2.4 Add a test that a voluntary browse-tab pick (`result.current.handleSelectTradeType(<Multipliers contract>)`) is preserved across a `rerender` that does **not** change `mock_contract_type` — i.e. `current_trade_type` stays Multipliers.
- [x] 2.5 Run `npm run test:jest -- packages/trader/src/AppV2/Hooks/__tests__/useMarketSelection.spec.tsx` and confirm the suite passes. Capture the red first (run the new resync test against the unmodified hook to confirm it fails for the intended reason — the browse trade type stays Rise/Fall after the store moves to Accumulators), then apply the fix from 1.1 and confirm green (per the cited-red convention; record the failing assertion in the PR).

## 3. Verification

- [x] 3.1 Run `npx -y @fission-ai/openspec validate market-info-page-displays-information-from-the-previously-selected-trade-type` and confirm it passes with no structural errors.
- [x] 3.2 Confirm `packages/trader/src/AppV2/Components/MarketSelection/market-selection-desktop.tsx` and `market-selection-mobile.tsx` need no code change — the fix is entirely in the shared hook; both shells already pass `current_trade_type`/`info_item`/`info_trade_type` through from the hook. (Verify by reading; no edit.)
- [x] 3.3 Confirm `packages/trader/src/AppV2/Components/MarketSelection/__tests__/market-info-screen.spec.tsx` needs no change — `MarketInfoScreen` is already driven by its `item`/`default_trade_type` props; the fix is upstream in `useMarketSelection`. (Verify by reading; no edit.)
- [x] 3.4 Run `npm run test:jest -- packages/trader/src/AppV2/Components/MarketSelection/__tests__/market-info-screen.spec.tsx` to confirm the info-screen suite still passes after the hook change.

## 4. Revision (QA feedback on #1174) — re-seed browse state on every open/close cycle

QA found the resync in section 1 insufficient: with Accumulators selected, browsing to Turbos without picking a market and closing leaves the abandoned Turbos tab in the hook's state (the store's `contract_type` never moved, so the resync effect never fired), and the next open shows Turbos instead of the still-selected Accumulators.

- [x] 4.1 Add an `is_open: boolean` input to `TUseMarketSelection` in `packages/trader/src/AppV2/Hooks/useMarketSelection.ts` and destructure it in the hook signature — the hook stays mounted across open/close cycles, so it needs the shell's open state to know a cycle happened.
- [x] 4.2 Widen the resync effect's deps to `[is_open, contract_type]` and extend its body to also reset the abandoned category and favourites tab (`setSelectedCategory('')`, `setIsFavouritesTab(false)`) alongside the existing browse-trade-type and info-state resets. Do **not** guard on `is_open` being true: running on the close edge means the reopened selector renders the active trade type from its first render (no flash of the abandoned tab, no symbol fetch for it), and the matching run on open sets identical values so React bails out. `getTradeTypeForContractType` returns a stable reference from the module-level `AVAILABLE_CONTRACTS`, so the bail-out holds.
- [x] 4.3 Pass `is_open: isOpen` from both shells: `market-selection-desktop.tsx` and `market-selection-mobile.tsx` (each already receives `isOpen` as a prop). This supersedes task 3.2 — the shells now need a one-line change each.
- [x] 4.4 In `packages/trader/src/AppV2/Hooks/__tests__/useMarketSelection.spec.tsx`, make `renderMarketSelection(is_open = true)` pass `is_open` through `renderHook`'s `initialProps` so a close/reopen cycle is `rerender({ is_open: false })` then `rerender({ is_open: true })`. Existing `rerender()` calls keep the current props (react-hooks defaults `newProps` to the previous props), so the section-2 tests are unaffected.
- [x] 4.5 Add `describe('browse state re-seeds on every open/close cycle')` covering: (a) the reported bug — Accumulators selected, browse Turbos, close, reopen → `current_trade_type` is Accumulators; (b) the re-seed lands on close, so `current_trade_type` is already Accumulators right after `rerender({ is_open: false })`; (c) an abandoned category (`'forex'`) and favourites tab reset on reopen; (d) an abandoned `info_symbol`/`info_trade_type` clear on reopen; (e) a voluntary Turbos pick survives further rerenders while still open.
- [x] 4.6 Capture the red: with the section-4 hook change reverted, the four new state-reset tests fail as `Expected: "Accumulators" / Received: "Turbos"`, `Expected: "" / Received: "forex"`, and `Received: "R_100"` — exactly the reported defect. With the fix applied, all 13 tests in the suite pass.
- [x] 4.7 Re-run the MarketSelection component suites (`packages/trader/src/AppV2/Components/MarketSelection`) after the shell changes — 13 suites / 78 tests pass.

## 5. Revision (review feedback on #1174) — fold search mode into the re-seed

Review found the re-seed from section 4 resets the browse tab, category, favourites tab and info state but not `is_searching`, which the mobile shell reads to render `MarketSearchPage` instead of the trade-type tabs. Only `handleClose` clears it, and the onboarding guide closes the selector via `setMarketSelectorOpen(false)` on the store (`GuideForPages/steps-config.tsx`, `guide-container.tsx`) without going through it — so a user who was searching lands back on the search page on the next open. The mobile shell has no counterpart to the desktop shell's own `!isOpen` reset effect.

- [x] 5.1 Add `setIsSearching(false)` to the re-seed effect in `packages/trader/src/AppV2/Hooks/useMarketSelection.ts`, so search mode resets on the open/close edge regardless of how the selector closed. `handleClose` keeps its own `setIsSearching(false)` (immediate, before the shell's `is_open` flips) and the desktop shell keeps its reset effect (it also clears the local `search_value` text) — both are now harmless duplicates.
- [x] 5.2 Drop the `eslint-disable-next-line react-hooks/exhaustive-deps` above the effect's deps array. Nothing in the body needs suppressing: `contract_type` is already declared, `getTradeTypeForContractType`/`AVAILABLE_CONTRACTS` are module-level imports, `useState` setters are stable, and `exhaustive-deps` permits an extra reactive dep like `is_open` on an effect. Confirmed clean with `npx eslint src/AppV2/Hooks/useMarketSelection.ts` from `packages/trader`.
- [x] 5.3 Add a test that search mode does not survive a close bypassing `handleClose`: `setIsSearching(true)`, then `rerender({ is_open: false })` / `rerender({ is_open: true })` → `is_searching` is `false`. Add its counterpart, that search mode is kept across a rerender while still open.
- [x] 5.4 Add the combined-sequence test from the review: `contract_type` changes while the selector is closed, then it reopens → `current_trade_type` is the new trade type. This exercises the reported sequence (switch trade type on the strip with the selector closed, then reopen) in one test rather than across two describe blocks.
- [x] 5.5 Capture the red: against the section-4 hook, the search-mode test fails as `Expected: false / Received: true` — the search page persists into the reopen. With 5.1 applied all 16 tests pass.
