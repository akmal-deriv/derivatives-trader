## Open Questions

Each has a **recommended default** already baked into the tasks below — the plan is complete and implementable as-is. A reviewer can override an answer, and only the named tasks change.

1. **Which tabs show the chevron?** (affects tasks 2.2, 4.2)
    - **(a) Active tab only — RECOMMENDED.** Only the active tab's tap opens the selector; an inactive tab's tap just activates it and a disabled tab's tap shows a snackbar, so a chevron there would signify the wrong action. Matches the Figma frame, where the chevron is on the expanded/active tab.
    - (b) Every tab that shows a label (i.e. all tabs on desktop, active only on mobile) — treats the chevron as "this strip is interactive" rather than "this tab opens the selector"; risks implying an inactive tab opens the selector.
    - (c) Every tab including disabled ones — rejected: a disabled tab cannot open the selector at all.

2. **Is the Figma's desktop `Rise/Fall ⌄ | +$12.98` divider in scope?** (affects task 3.3)
    - ~~**(a) Out of scope — RECOMMENDED.** The strip already ships inline P/L with a `•` separator (`.market-tab__profit-dot`), which is also present in the Figma copy list. Swapping `•` for a 1×12px rule is a separate cosmetic change with no bearing on the discoverability problem this issue reports. Issue #1050 itself flags it as "worth confirming whether that's in scope here or separate".~~
    - **(b) In scope — ANSWERED (b), reversing the (a) recommendation.** Replace the `•` with a 1px × 12px divider using `--semantic-color-monochrome-border-normal-mid`, desktop only.
    - **Resolution (2026-08-18):** QA's Figma comparison raised the surviving `•` as a confirmed mismatch — sub-issue #1168 under #1050. The scope call in (a) is therefore overturned by review, and task 3.3 was reworked to implement (b). See task 3.6.

3. **Chevron colour token.** (affects task 2.3)
    - **(a) Inherit the subtitle's `--component-textIcon-normal-default` — RECOMMENDED.** The chevron reads as part of the trade-type row, which is what the approved Design 2 depicts.
    - (b) `--component-textIcon-normal-prominent` (as `profit-loss-pill.tsx` uses) — higher contrast, directly addresses issue #1050's open point 1 that the caption row is the lower-contrast line. Escalate to this if design judges (a) too faint at device scale during task 5.1.

## 1. Confirm the design source

- [ ] 1.1 **BLOCKED — not verifiable in this session.** Open Figma [node 21702:539617](https://www.figma.com/design/zkhQsTI8PMKJX7k1N2QBJ1/Akram---Multiple-charts?node-id=21702-539617) (`Tabs with chevron`) and confirm against the current build: 12px `chevron-down`, on the trade-type row only, positioned before the inline P/L. The Figma could not be read first-hand while planning, so this is a read-only verification step before coding — if it contradicts `design.md`, follow the Figma and note the divergence.
    - The Figma MCP read was again unauthorised (non-interactive session, no OAuth flow available), exactly as during planning. Implementation therefore follows `design.md` as written: 12px `LabelPairedChevronDownCaptionRegularIcon`, trade-type row only, before the inline P/L. **Still needs a human to open the node and confirm** — any divergence is a follow-up, not a rework of the structure.

## 2. Render the chevron in `MarketTab`

- [x] 2.1 In `packages/trader/src/AppV2/Components/MarketTabs/market-tab.tsx`, import `LabelPairedChevronDownCaptionRegularIcon` from `@deriv/quill-icons` (12px caption chevron; same icon as `AppV2/Components/ChartProfitLoss/profit-loss-pill.tsx`).
- [x] 2.2 Inside the `trade_type_title &&` block, wrap the trade-type text in its own element (`<span className='market-tab__trade-type'>{trade_type_title}</span>`) and render the chevron immediately after it — before the existing `.market-tab__profit` span — gated on `is_active` (per Open Question 1a).
    - **Corrected gate (review finding B1):** `is_active` alone was wrong. While an automation run locks the strip, `MarketTabs.handleSelect` returns early with the "Tab switching is locked until automation is stopped." snackbar _before_ its open-the-selector branch, yet the running tab is still `is_active` with `is_disabled === false` (pinned by `market-tabs.spec.tsx`'s lock test). So the cue rendered on the one tab whose tap cannot open the picker. `MarketTab` now takes an `opens_selector` prop (defaulting to `is_active`) and gates the chevron, `aria-haspopup` and the `aria-label` on it; `MarketTabs` passes `is_active && !is_automation_market_locked`. `design.md`'s Decision and the capability spec's "appears only where tapping opens the selector" requirement were updated to match.
- [x] 2.3 Wrap the chevron in `<span className='market-tab__chevron' data-testid='dt_market_tab_chevron' aria-hidden='true'>` and give the icon `fill='var(--component-textIcon-normal-default)'` (per Open Question 3a). The `data-testid` is what tests assert on, so they don't couple to quill-icons' SVG markup.
- [x] 2.4 Add `aria-haspopup='dialog'` on the tab root (`div.market-tab`) when `is_active` — the market selector renders as a mobile full-screen modal / desktop popover.
    - Gated on `opens_selector`, not `is_active` — see the note under 2.2. Same for the 2.5 `aria-label`, so a locked tab never announces an action its tap won't perform.
- [x] 2.5 Give the active tab an `aria-label` naming the action, e.g. `localize('Change market and trade type: {{market}}, {{trade_type}}', { market: getSymbolDisplayName(market.symbol), trade_type: trade_type_title })` from `@deriv-com/translations` (already imported in `market-tabs.tsx`; add the import here). Fall back to the market name alone when there is no `trade_type_title`, and leave non-active tabs without an `aria-label` so their text content is announced as today.
    - Shipped strings (checked against the Deriv Content Style Guide — sentence case, active voice, no em dash, no banned terms): `Change market and trade type: {{market}}, {{trade_type}}` and the fallback `Change market: {{market}}`. `getSymbolDisplayName(market.symbol)` was hoisted to `market_display_name` and reused by the name line.

## 3. Style the subtitle row (`market-tabs.scss`)

- [x] 3.1 In `packages/trader/src/AppV2/Components/MarketTabs/market-tabs.scss`, make `.market-tab__subtitle` a flex row: `display: flex; align-items: center; gap: var(--core-spacing-200, 4px);` and add `.market-tab__trade-type { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; min-width: 0; }` plus `.market-tab__chevron { flex-shrink: 0; display: inline-flex; align-items: center; }`. Keep `.market-tab__profit`'s existing `flex-shrink: 0` behaviour (add it if absent) so neither the chevron nor the P/L is squeezed.
    - `flex-shrink: 0` was absent on `.market-tab__profit`, so it was added. Its `margin-inline-start: var(--core-spacing-200, 4px)` was removed at the same time: the subtitle's new 4px `gap` now supplies that spacing, and keeping both would have doubled it to 8px. The gap is also the better mechanism here — it collapses with the P/L on mobile, where `.market-tab__profit` is `display: none`.
- [x] 3.2 In the `.market-tabs--desktop` block, narrow the `&__text > *` truncation rule so its `overflow: hidden; text-overflow: ellipsis` applies to the market-name line but no longer collapses the subtitle box (which now owns its own overflow). The market name MUST still truncate — this is the main regression risk called out in `design.md`.
    - Narrowed to `&__text > *:not(.market-tab__subtitle)`, so the market-name line keeps its ellipsis verbatim and only the subtitle is exempted. Needs the visual confirmation in 5.2.
- [x] ~~3.3 Leave the `•` P/L separator (`.market-tab__profit-dot`) as-is (per Open Question 2a). No divider work in this change.~~
    - **Superseded by task 3.6** — Open Question 2 was re-answered (b) after QA's Figma comparison (#1168).
- [x] 3.4 Verify no change is needed in the mobile block: inactive tabs collapse the whole `market-tab__reveal` to `0fr`, so the chevron is hidden there by the existing reveal mechanism — the `opens_selector` gate in 2.2 is what actually keeps it out of the DOM.
    - Confirmed by reading the mobile block: no rule references the subtitle's internals, and the reveal collapse is unchanged. No mobile-block edit made.
- [x] 3.5 Run `npm run test:stylelint` (or `npm run stylelint:fix`) on the changed SCSS.
    - `npx stylelint packages/trader/src/AppV2/Components/MarketTabs/market-tabs.scss` → clean (exit 0). Scoped to the changed file rather than the whole suite, which the separate verification stage runs.
- [x] 3.6 (added — QA finding #1168, replaces 3.3) Swap the `•` P/L separator for the Figma's 1×12px vertical divider, per the re-answered Open Question 2b.
    - `market-tab.tsx`: `<span className='market-tab__profit-dot'>•</span>` → `<span className='market-tab__profit-separator' data-testid='dt_market_tab_profit_separator' aria-hidden='true' />`. Drawn in CSS rather than as a `|` glyph — the issue allowed either, but a rule is what the Figma specifies (1×12px) and it keeps the tab's `textContent` free of decorative characters, which is the precondition the E2E `hasText`/`toHaveText` locators rely on (see 6.1). Same reason the `•` was worth removing rather than swapping for `|`.
    - `market-tabs.scss`: `&__profit-dot { color: … }` → `&__profit-separator { flex-shrink: 0; width: 1px; height: 1.2rem; background: var(--semantic-color-monochrome-border-normal-mid); }`. The token is the one the issue names and the one the existing inter-tab divider already uses (`.market-tab:not(.market-tab--active):not(:first-child):before`), so light/dark both come from the theme. `1.2rem` = 12px at this repo's 10px root, matching the neighbouring `3.2rem`/`5.6rem` conventions.
    - Placement is unchanged from the `•`: still the first child of `.market-tab__profit`, i.e. after the chevron and before the amount. Keeping it _inside_ that span is what satisfies two of the issue's ACs for free — `.market-tab__profit` is `display: none` off `.market-tabs--desktop` (desktop only), and the whole span is gated on `has_profit` (no orphaned divider on a tab with no position).
    - No change to the chevron, the truncation rules, or the `opens_selector` gate — the divider inherits `flex-shrink: 0` behaviour from its own rule, so it stays whole on a shrunk tab exactly as the amount does.
    - No user-facing copy changed (the divider renders no text), so the Content Style Guide has nothing to bite on here.
    - `npx stylelint` on the changed SCSS → clean; `npx eslint` + `npx prettier --check` on all three changed files → clean.

- [x] 3.7 (added — QA finding #1175, layout regression from 3.1/3.2) Clip the subtitle row to the tab: `.market-tab__subtitle` gains `min-width: 0; overflow: hidden;`.
    - 3.1 made the row a flex box and 3.2 exempted it from the desktop `&__text > *` ellipsis rule, which together removed the only thing clipping that row. The market-name line still clips itself, so the row became the tab's one unclipped line — and its chevron, divider and P/L amount are all `flex-shrink: 0`. Past the point where `.market-tab__trade-type` has given up all its width (a desktop strip beyond 4 tabs, where `flex: 0 1 auto` shrinks each tab), the row's content is wider than the tab and spilled over the neighbouring tab. QA saw it as the P/L price overlapping the strip.
    - The clip belongs on the row, not on `.market-tab`: the tab paints its `&--active:before`/`:after` corner fillets _outside_ its own box (`inset-inline-start: calc(-1 * …)`), so `overflow: hidden` there would square off the active tab against the chart.
    - Declared on the base rule rather than under `.market-tabs--desktop`, so it is a property of the row wherever it renders. It costs mobile nothing: `.market-tab` is `flex-shrink: 0` there and the P/L is `display: none`, so the row never exceeds its tab.
    - `min-width: 0` accompanies it because a flex item's automatic minimum size would otherwise floor the row at its content width — the same reason `.market-tab__trade-type` carries it.
    - Order of degradation is unchanged, so #1050's cue requirement still holds: the trade-type text is the only shrinkable part, so it ellipsises first and the chevron + amount stay whole while it still has width to give. The active tab (`flex: 0 0 auto`, 26.4rem) never shrinks, so its cue and amount are never reached by the clip.
    - No change to the chevron, the divider, the `opens_selector` gate or the tab-count caps. `npx stylelint` on the changed SCSS → clean.
    - **Superseded by task 3.8** — 3.8 removes the flex row entirely, which removes the spill this clip was patching, so the clip is gone with it.

- [x] 3.8 (added — QA finding, revises 3.1/3.2/3.7) Restore production's truncation for the trade-type row: it truncates as ONE text line, so the trade type keeps its width and the chevron + P/L run off the end.
    - QA compared the two builds at 5 open tabs: production shows the trade type alone (`Rise/Fall`, the P/L dropped); this branch showed `R… | +412.98 USD` — the flex row from 3.1 made the trade-type span the only shrinkable part, so the text was squeezed to a leading character or two while the cue and amount were held whole. That inverts the priority: a tab exists to say which market and which trade type, so the secondary parts are what should go first.
    - `market-tabs.scss`: `&__subtitle` drops `display: flex`, `align-items`, `gap`, `min-width: 0` and 3.7's `overflow: hidden`, keeping only its colour (its production declaration). `&__trade-type` is deleted. `&__chevron` drops `flex-shrink: 0` and gains `margin-inline-start: var(--core-spacing-200, 4px)` + `vertical-align: middle`; `&__profit` likewise (its `margin-inline-start`, removed by 3.1 in favour of the gap, comes back). `vertical-align` matters because an inline box whose content has no baseline — the icon-only chevron, and the P/L whose first child is now the rule from 3.6 — otherwise sits on the text baseline and rides high.
    - `market-tabs--desktop`: `&__text > *:not(.market-tab__subtitle)` reverts to `&__text > *`, so both label lines ellipsise again. That rule is also what re-contains the row, which is why 3.7's clip is no longer needed: the box that clips is the subtitle itself, still not `.market-tab` (whose corner fillets paint outside its box).
    - `market-tab.tsx`: the `.market-tab__trade-type` span is gone — the trade type is bare text on the row again, exactly as in production. DOM order (trade type → chevron → divider → amount) is now what decides the order of degradation, so it is asserted directly.
    - Accepted consequence: the chevron can now truncate away on a narrow tab. It renders only on the ACTIVE tab, which is `flex: 0 0 auto` on desktop and content-width on mobile — neither shrinks — so it is never reached in practice. #1050's cue requirement holds.
    - `npx stylelint` on the changed SCSS → clean.

## 4. Tests

- [x] 4.1 In `packages/trader/src/AppV2/Components/MarketTabs/__tests__/market-tab.spec.tsx`, add a test asserting the chevron (`dt_market_tab_chevron`) is present when `is_active` — cite the red run per `.buildwright/framework/tdd-evidence.md` (it must fail before task 2 lands).
    - Red (run against unfixed code, before task 2): `MarketTab › chevron cue › shows the chevron on the trade-type row of the active tab` — `Unable to find an element by: [data-testid="dt_market_tab_chevron"]`. Two more went red for the same reason: `… › fires onSelect once when the chevron itself is tapped` and `… › hides the chevron from assistive tech and announces the action on the active tab`. Suite at red: 3 failed, 17 passed.
- [x] 4.2 Add tests asserting the chevron is absent for an inactive tab and for a disabled tab.
    - These two are absence assertions, so they were **green from birth** (no chevron existed yet) — they are not red→green evidence. Proven to bite instead by mutation: replacing the `is_active` gate with `true` after the Green step failed exactly these two (`shows no chevron on an inactive tab`, `shows no chevron on a disabled tab`) and nothing else. Gate restored.
- [x] 4.3 Add a test asserting no chevron renders when the trade type resolves to no display name (pass a `contract_type` with no config title / no AppV2 trade type).
    - Uses `contract_type: 'not_a_contract_type'`. Also an absence test, green from birth (characterization).
- [x] 4.4 Add a test that clicking the chevron fires `onSelect` exactly once with the market (chevron is decorative, the tab is the tap target — no `stopPropagation`, unlike the remove "✕").
- [x] 4.5 Add tests for the a11y contract: the active tab has `aria-haspopup='dialog'` and an `aria-label` containing both the market display name and the trade type; a non-active tab has neither.
    - Asserted via `toHaveAccessibleName(expect.stringContaining(...))` for each value, so the test does not pin the exact sentence and copy can be revised without churn. The non-active half is characterization (green from birth).
- [x] 4.6 Confirm the existing subtitle assertions still pass unchanged (`getByText('Vanillas')` in `market-tab.spec.tsx`, and any strip-level text queries in `market-tabs.spec.tsx`); update only if the extra span genuinely breaks a query.
    - No test churn: every pre-existing assertion passes untouched. One test was **added** here beyond the plan — `adds no text content, so text-based tab locators keep matching`. It pins that the active tab's `textContent` is byte-identical with and without the cue, which is the precondition the E2E strip specs rely on (see 6.1); worth locking down rather than assuming the icon renders no `<title>`.
- [x] 4.7 Run `npm run test:jest -- packages/trader/src/AppV2/Components/MarketTabs` and confirm green.
    - Run as `npx jest packages/trader/src/AppV2/Components/MarketTabs` (the `npm run` script shells out to a bare `jest`, which is not on PATH here). **3 suites, 51 tests, all passing.**
- [x] 4.8 (added — review finding B1) Cover the locked-strip case at both levels: in `market-tab.spec.tsx`, an `is_active` tab with `opens_selector={false}` renders no chevron, has no `aria-haspopup`/`aria-label`, keeps its active class + trade-type text, and still forwards the tap to `onSelect` (the parent turns it into the snackbar); in `market-tabs.spec.tsx`, the lock test asserts the running tab is handed `opens_selector === false` while the unlocked active tab gets `true`.
    - Red proven by mutation (per `.buildwright/framework/tdd-evidence.md`): reverting all three gates in `market-tab.tsx` to `is_active` failed exactly the new unit test (`shows no chevron on an active tab whose tap does not open the selector`) and nothing else — 1 failed / 21 passed. Gates restored → **3 suites, 52 tests, all passing.**

- [x] 4.9 (added — QA finding #1168) Cover the divider in `market-tab.spec.tsx`: it renders between the chevron and the amount, contributes no text (and no `•`), is `aria-hidden`, and is absent when there is no P/L.
    - Red (run against the `•` code, before 3.6): `MarketTab › P/L separator › separates the chevron from the P/L with a divider rule, not a bullet glyph`, `… › places the divider after the chevron and before the P/L amount` and `… › renders the divider for a loss too` all failed on `Unable to find an element by: [data-testid="dt_market_tab_profit_separator"]`. Suite at red: **3 failed, 23 passed**. Green after 3.6: **3 suites, 56 tests, all passing.**
    - The fourth test (`renders no divider when there are no open positions (no orphaned separator)`) is an absence assertion, so it was green from birth — not red→green evidence. Proven to bite by mutation per `.buildwright/framework/tdd-evidence.md`: hoisting the separator out of the `has_profit &&` gate so it renders unconditionally failed it (plus the three above, on `getByTestId` ambiguity) — 4 failed / 22 passed. Gate restored.
    - Ordering is asserted with `compareDocumentPosition`, not by reading `textContent`: the divider is now text-free, so the row's `Rise/Fall ⌄ | +$412.98` reading is a DOM-order fact rather than a string one. `expect(tab).not.toHaveTextContent('•')` is what pins the bullet as actually gone rather than merely joined by a rule.

- [x] 4.10 (added — QA finding #1175) Cover the clip. jsdom does no layout, so the containment itself is asserted against the stylesheet in a new `__tests__/market-tabs-layout.spec.ts`: the row declares `overflow: hidden` + `min-width: 0`, the trade-type span is still the only shrinkable part (`min-width: 0` + ellipsis, against `flex-shrink: 0` on the chevron and the P/L), the market-name line still clips itself, and `.market-tab` does _not_ clip (which would cut the active tab's corner fillets). Comments are stripped before matching so the prose explaining a rule cannot satisfy an assertion about it.
    - Red (run against the unfixed SCSS): `market tab trade-type row layout › clips the row to the tab, so the inline P/L cannot paint over the neighbouring tab` failed on the missing `overflow: hidden` — 1 failed / 3 passed. The other three are characterization: they pin the surrounding contract this fix must not break (the shrink order from 3.1, the market-name ellipsis from 3.2, the fillets from the desktop block), and each names the rule it protects.
    - Paired with a DOM-level test in `market-tab.spec.tsx` (`keeps the amount inside the trade-type row, which is what clips it to the tab`): the amount and the divider must render _inside_ `.market-tab__subtitle`, since the stylesheet clip only covers what is in that box. Structural, so it needs `closest()` — same `testing-library/no-node-access` disable + rationale as the 4.1 chevron assertion.
    - Full suite after the fix: **4 suites, 61 tests, all passing.**

- [x] 4.11 (added — QA finding, revises 4.10) Re-point `__tests__/market-tabs-layout.spec.ts` at the restored truncation, since its three flex-row assertions pinned exactly the behaviour 3.8 removes.
    - Now asserts: the desktop rule ellipsises **every** label line with no `:not(…)` exemption (the exemption is what let the text shrink instead); the row declares no `display: flex` and no `flex-shrink` on any of its parts (either would mean the flex row was back); and the cue and amount are spaced with `margin-inline-start` and centred with `vertical-align: middle`. The fillet assertion is unchanged.
    - `market-tab.spec.tsx`'s chevron test drops `toHaveClass('market-tab__trade-type')` (the span is gone) for the order fact that replaces it: the row's first node is the trade-type text and its last element is the chevron — which is what makes the cue, not the text, the part that truncates away.
    - Red (layout spec run against the pre-fix SCSS): **3 failed / 1 passed**, each on a declaration 3.8 introduces. Green after: **4 suites, 61 tests, all passing.**

## 5. Verify

> **5.1–5.4 are human visual checks and are NOT done.** They need a running app on a real viewport, which this unattended implementation session has no way to drive. Each is left unchecked deliberately — do not read the rest of the plan being complete as these having passed. 5.1 in particular is the gate on Open Question 3b (escalate the chevron to `--component-textIcon-normal-prominent` if `-default` reads too faintly), so it still needs design's eye.

- [ ] 5.1 Manual check per issue #1050, mobile viewport (390px): the active tab shows `<market>` / `<trade type> ⌄`; inactive tabs stay icon-only squares; the chevron reads clearly as a "tap to change" cue at real device scale. If it reads too faintly, escalate the colour token per Open Question 3b and get design sign-off.
- [ ] 5.2 Manual check on desktop: open 5+ tabs so tabs shrink — **the gate on 3.8.** Side by side with production, each shrunken tab reads the same way: its trade-type text stays readable (ellipsising only if the text alone overflows) and the P/L amount is dropped, rather than a one-character trade type next to a whole amount. The market-name line still truncates, the ACTIVE tab still shows its chevron and amount in full, and at 4, 5, 6 and 7 tabs with a running position nothing overlaps the strip or a neighbouring tab. Check RTL and both themes here too.
- [ ] 5.3 Manual check that behaviour is unchanged: tapping an inactive tab switches to it (no selector); tapping the active tab opens the selector to replace that pair; the "✕" still removes without opening the selector; a disabled tab (e.g. an unsupported trade type in Automate) shows no chevron and still surfaces its snackbar.
    - Partially covered by the unit suite: the disabled-tab half (no chevron, `onDisabledClick` still fires instead of `onSelect`) and the "✕ removes without selecting" half are asserted in `market-tab.spec.tsx`. The selector-opening halves live in `MarketTabs.handleSelect`, untouched by this change. The visual pass is still owed.
- [ ] 5.4 Check both themes (light/dark) — the chevron uses a semantic token, so confirm it renders visibly in dark mode too. **Also covers the 3.6 divider** (`--semantic-color-monochrome-border-normal-mid`): confirm the 1×12px rule is visible in both themes and reads as `<trade type> ⌄ | +$…` against the Figma, and that design (@lucas / @ashkan) signs the swap off per #1168.
- [x] 5.5 Run `npm run test:eslint-all` (or the trader-scoped lint) on the changed files.
    - `npx eslint` on `market-tab.tsx` + `market-tab.spec.tsx` → clean. Scoped to the changed files rather than all packages, which the separate verification stage covers. The first pass flagged `testing-library/no-node-access` on the `chevron.closest('.market-tab__subtitle')` structural assertion; resolved with an inline `eslint-disable-next-line` + rationale, matching existing precedent in this repo (`discovery-section.spec.tsx`, `input-popover.spec.tsx`, `trade-params-footer.spec.tsx`), since no role/text query can express "the cue is on the trade-type row". `npx prettier --check` also clean on every changed file.

## 6. E2E and docs impact

- [x] 6.1 Confirm the existing Playwright locators still resolve: `playwright/pages/TradeBasePage.ts:166` (`[data-testid="dt_market_tab"][aria-current="true"]`), `playwright/pages/MarketSelectionPage.ts:382`/`392` (`marketTabs` / `marketTab(market, tradeType)` via `hasText` filters) and `:406` (`getByRole('button', { name: 'Remove market' })`). None keys off the tab's accessible name, so the new active-tab `aria-label` should be non-breaking — verify rather than assume, since the `aria-label` does change the tab root's accessible name.
    - Verified non-breaking, by audit of every market-tab locator and its call sites:
        - `activeMarketTab` (`TradeBasePage.ts:166`) — attribute selector on `data-testid` + `aria-current`; neither attribute changed.
        - `marketTabs` / `marketTab(market, tradeType)` (`MarketSelectionPage.ts:382`/`392`) — `hasText` matches **text content**, not accessible name. The new `<span class='market-tab__trade-type'>` wrapper preserves the concatenated text, and the chevron is an SVG contributing none.
        - `removeMarketTabButton` (`:406`) and `verify-trade-tabs.spec.ts:117` — scoped `getByRole('button', { name: 'Remove market' })`. The tab root was already `role='button'`; its accessible name changes from its text to the new `aria-label`, but that label is never `'Remove market'`, so no new match and no ambiguity.
        - `verify-trade-tabs.spec.ts:26`/`39` — `textContent()` captured, then compared with `toHaveText`, which uses `textContent` by default (`useInnerText` is `false`). This was the one genuine risk (flexing the subtitle blockifies its children, which _would_ change `innerText`), so it is now pinned by the unit test added in 4.6, which proves the tab's `textContent` is identical with and without the cue.
- [ ] 6.2 **BLOCKED — not runnable in this session.** Run the market-tab E2E spec `playwright/tests/market-selection/verify-trade-tabs.spec.ts` (desktop + `chromium-mobile`) and confirm no regression. Per `playwright/CLAUDE.md`, do not modify files outside `playwright/` to satisfy a test.
    - Requires a live staging app plus `TEST_EMAIL`/`TEST_PASSWORD`; `playwright/.env.staging` is gitignored and absent here, and `ENV_ENCRYPTION_KEY` is not available to decrypt `.env.staging.enc`. Chromium itself is installed, so this only needs credentials to run. The static analysis in 6.1 is what currently stands in for it — **it is not a substitute for the run.**
- [x] 6.3 Update `playwright/flows/market-selection/flow.md` — the tab-strip flows (notably Flow 1b / Flow 2, "clicking the already-active tab opens the picker") now have a documented visual cue; record the chevron affordance and the active tab's `aria-haspopup`/`aria-label` as expected results, and add the gap to `coverage.md` / `catalog.md` if a new assertion is warranted. This is the documentation half of "Documentation Is Part of Done" (`.buildwright/steering/philosophy.md`); no README or user-facing doc describes the market tab today, so state that in the final report.
    - `flow.md`: added a callout under Shared Step 1b describing the cue, its testid, the active-only rule and the a11y attributes — including the warning that the `aria-label` overrides the tab's accessible name, so tabs must be located by testid / `aria-current` / visible text. Also amended Flow 2 step 2 and Flow 20 step 1 (which now records the truncation contract: trade-type text ellipsises, chevron and P/L do not).
    - New gap **G2** ("Active tab's chevron cue marks it as the picker trigger") added to `coverage.md` Sections 1 and 2, to `catalog.md`'s journey index, and as a 4-case Gap Flow in `flow.md`. Proposed home is an extension of the existing `verify-trade-tabs.spec.ts` rather than a new spec, since Flow 20 already walks the active/inactive split. **Not implemented** — writing it would need the live-app exploration `playwright/CLAUDE.md` mandates before touching a Page Object, which this session cannot do.
    - Confirmed no README or user-facing doc describes the market tab, so there is nothing else to update.
