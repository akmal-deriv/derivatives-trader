name: DocSync AI - Documentation Sync

on:
  schedule:
    - cron: "15 7 * * 5" # 11:15 AM GST (7:15 AM UTC) every Friday

  issue_comment:
    types: [created]

jobs:
  docsync-scheduled:
    if: github.event_name == 'schedule'
    permissions:
      contents: write
      pull-requests: write
      issues: write
    uses: deriv-com/shared-actions/.github/workflows/docsync-ai.yml@master
    with:
      trigger_type: "scheduled"
      base_branch: "master"
      slack_channel_id: ${{ secrets.DOCSYNC_SLACK_CHANNEL_ID }}
      slack_mention_user_id: ${{ secrets.DOCSYNC_SLACK_MENTION_USER_IDS }}
    secrets:
      DOCSYNC_GITHUB_TOKEN: ${{ secrets.DOCSYNC_GITHUB_TOKEN }}
      DOCSYNC_ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      DOCSYNC_SLACK_TOKEN: ${{ secrets.DOCSYNC_SLACK_TOKEN }}
  docsync-comment:
    if: |
      github.event_name == 'issue_comment' &&
      github.event.issue.pull_request &&
      startsWith(github.event.comment.body, '@docbot')
    permissions:
      contents: write
      pull-requests: write
    uses: deriv-com/shared-actions/.github/workflows/docsync-ai.yml@master
    with:
      trigger_type: "comment"
      comment_body: ${{ github.event.comment.body }}
      comment_pr_number: ${{ github.event.issue.number }}
      comment_id: ${{ github.event.comment.id }}
      base_branch: "master"
      slack_channel_id: ${{ secrets.DOCSYNC_SLACK_CHANNEL_ID }}
      slack_mention_user_id: ${{ secrets.DOCSYNC_SLACK_MENTION_USER_IDS }}
    secrets:
      DOCSYNC_GITHUB_TOKEN: ${{ secrets.DOCSYNC_GITHUB_TOKEN }}
      DOCSYNC_ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      DOCSYNC_SLACK_TOKEN: ${{ secrets.DOCSYNC_SLACK_TOKEN }}
